import { VStack , Heading, Text , FlatList} from 'native-base';

export const MyAdsDetails = ()=>{
    //params
    // if isActive === false product photo is darker and replace button Deactivate Ad to Activate Ad
    const isActive = true;


    return (
        <VStack>
            <Heading>9 Ads</Heading>
        </VStack>
    );
};